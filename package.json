{
  "name": "netsettle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "contracts",
    "app"
  ],
  "scripts": {
    "build": "npm run build --workspace contracts && npm run build --workspace app",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "npm run lint --workspace contracts && npm run lint --workspace app",
    "typecheck": "npm run typecheck --workspace contracts && npm run typecheck --workspace app",
    "test": "npm run test --workspace contracts && npm run test --workspace app",
    "test:nox": "npm run test:nox --workspace contracts"
  },
  "devDependencies": {
    "@eslint/js": "9.39.4",
    "eslint": "9.39.4",
    "globals": "17.6.0",
    "prettier": "3.8.3",
    "typescript-eslint": "8.59.3"
  }
}
